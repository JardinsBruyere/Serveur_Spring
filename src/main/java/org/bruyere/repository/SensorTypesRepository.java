package org.bruyere.repository;

import org.bruyere.entity.SensorTypes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SensorTypesRepository extends JpaRepository<SensorTypes,Integer> {
}
